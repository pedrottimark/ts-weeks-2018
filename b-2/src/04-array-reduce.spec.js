describe('accumulate sum (addition) of items', () => {
    const addItems = items => items.reduce((sum, item) => /* TODO */, 0)

    it('returns 0 as sum of empty array', function () {
        // TODO write a test case
    })
    it('returns sum of non-empty array', function () {
        // TODO write a test case
    })
})

describe('return a maximum length of strings', () => {
    const maxLength = items => items.reduce((max, item) => /* TODO */, 0)

    it('returns 0 as maximum length of empty array', function () {
        // TODO write a test case
    })
    it('returns maximum length of non-empty array', function () {
        // TODO write a test case
    })
})
